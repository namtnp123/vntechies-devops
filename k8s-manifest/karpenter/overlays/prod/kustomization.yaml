apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

bases:
  - ../../base

patchesStrategicMerge:
  - patches.yaml

commonLabels:
  environment: prod
  managed-by: kustomize
