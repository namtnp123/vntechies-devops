server:
  # ClusterIP — external traffic comes through the ALB Ingress, not a LoadBalancer service.
  service:
    type: ClusterIP
  extraArgs:
    - --insecure
configs:
  params:
    server.insecure: "true"
